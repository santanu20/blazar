<!-- /doc verified against 9e34bafc9f9dc2a59fa2fa81a49b4da9d12feba0 on 2026-09-18 -->

# 7. SETUP

> Verified against Blazar v0.6.1 source. **INFERRED** tags mark reasoned statements.

## Environment variables (NAME + PURPOSE only)

### Product runtime (read by `blazar` itself)

| Variable | Purpose |
|---|---|
| `BLAZAR_KEYS` | bootstrap API keys into config at startup |
| `BLAZAR_REGISTRY_BASE` | override the model registry base URL |
| `BLAZAR_REGISTRY_TOKEN` | access token for private HF namespaces |
| `BLAZAR_GH_BASE` | override the GitHub base URL for engine downloads |
| `BLAZAR_REPO` | override the release repository for downloads |
| `BLAZAR_INSTALL_BASE_URL` | override the engine artifact download base URL |
| `BLAZAR_ENGINE_PATH` | point at an engine binary location |
| `BLAZAR_SYSTEMCTL` | override the systemctl binary used for service management |
| `BLAZAR_CAPABILITY_REGISTRY` | override (or, when empty, disable) the capability-lane registry URL |
| `BLAZAR_ENGINE_GATE` | influence the engine update regression gate |
| `BLAZAR_HOST` | target host for CLI client verbs against a running daemon |
| `BLAZAR_DEFAULT_CTX` | override the default context size |
| `BLAZAR_CHILD_TRANSPORT` | override `child_transport` (`tcp` / `unix`) |
| `RUST_LOG` | log filter; wins over config `log_level` (escape hatch) |

### Installer (read by `scripts/install.sh` / `bootstrap.sh` / `uninstall.sh`)

`BLAZAR_REPO`, `BLAZAR_VERSION`, `BLAZAR_INSTALL_BASE_URL`, `BLAZAR_INSTALL_ENGINE`, `BLAZAR_INSTALL_MODEL`, `BLAZAR_BOOTSTRAP`, `BLAZAR_FORCE_BOOTSTRAP`, `BLAZAR_AUTO_BOOTSTRAP`, `BLAZAR_AUTO_DRIVER`, `BLAZAR_CHECKOUT`, `BLAZAR_SUDO`, `BLAZAR_SYSTEM_BIN_DIR`, `BLAZAR_SYSTEMCTL`, `BLAZAR_UNIT_PATH`, `BLAZAR_UNIT_MEMORY_HIGH`, `BLAZAR_SERVICE_USER`, `BLAZAR_SERVICE_GROUP` — all override the corresponding installer default (release source, version pin, download base, bootstrap behavior, driver install, checkout dir, privilege helper, unit-file location/memory limit, service user/group).

### Internal test harness (NOT part of the product interface)

A `BLAZAR_VALIDATE*` / `BLAZAR_STUB_*` variable family plus `BLAZAR_TEST_MTP_GGUF` exists for the repository's own validation harness (`scripts/validate.py`). Values and semantics are internal to the test suite and intentionally not documented.

## Local development setup

1. **Toolchain:** Rust (stable). On a fresh source checkout, a zero-arg `cargo build` works; `scripts/bootstrap.sh --minimal` provisions a C compiler + Rust toolchain first (announced before acting).
2. **Clone and build:**
   ```sh
   git clone <repo> && cd blazar
   cargo build --release
   # binary: target/release/blazar
   ```
3. **Paths the daemon uses** (created on demand by `dirs.ensure()`):
   - `~/.config/blazar/config.toml` — configuration
   - `<XDG data>/blazar/` — `blazar.db`, `models/`, `engines/`, `run/` (per-child pid + child auth secret files, `daemon.log` — the daemon's own durable log, rotated to `daemon.log.1` at 10 MiB — and per-child `.sock` under `child_transport = "unix"`), `speccache/`, `sessions/`
   - Engine rows in `blazar.db` record their server binary **relative to the data dir** (`engines/<tag>/...`), so a data dir move/copy stays relocatable; the out-of-tree `local` lane (`BLAZAR_ENGINE_PATH`) keeps its absolute path.
   - XDG variables absent → falls back to CWD **with a loud stderr warning** (never silent).
4. **First engine + model:** `blazar engine update` then `blazar pull qwen3-0.6b`.

## Running the test suite

| Item | Command / fact |
|---|---|
| Unit + integration tests | `cargo test --workspace` (per-crate: `cargo test -p blazar-gateway` etc.) |
| Integration suites | per-crate `tests/` dirs — e.g. `blazar-gateway/tests/` (`gateway.rs`, `semcache.rs`, `sentinel.rs`, `structured_output.rs`, `sessions.rs`, `compat.rs`, `crash.rs`), `blazar-runtime/tests/` (`supervisor.rs`, `engine_manager.rs`, `tuner.rs`, `mistralrs.rs`, `replicas.rs`, `quantize.rs`, `models_alias.rs`, `engine_build.rs`), `blazar-cli/tests/` (`upgrade.rs`, `config_set.rs`) |
| Engine stubs | workspace-root `tests/fixtures/` — `stub_llama_server.rs`, `stub_llama_bench.rs`, `stub_llama_quantize.rs` (exposed behind the `test-util` feature; tests spawn stubs instead of real engines) |
| Full harness | `scripts/validate.py` — the repository's end-to-end validation harness (internal; drives real gateway + stubs via the `BLAZAR_VALIDATE*` env family) |
| Matrix scripts | `scripts/bench_matrix.py`, `scripts/engine_coverage.py` |
| Interpreting results | standard `cargo test` output; suite count: 27 workspace test suites at v0.6.1 |

## Build process

- `cargo build --release` — workspace build (4 crates, resolver 2, edition 2021).
- `unsafe_code = deny`, clippy pedantic in CI (**INFERRED**: enforced via repo lint config).
- Engine binaries are NOT built by the workspace by default — `blazar engine update` downloads official artifacts (sha256-verified, regression-gated); `blazar engine build cuda|cpu` builds them locally (auto GPU-arch detection via `nvidia-smi`, host-compiler matching for nvcc, installs `bNNNN-cuda`-style tags with `llama-server`/`-quantize`/`-imatrix`/`-perplexity`/`-bench` binaries).

## Deployment flow

1. **End-user install (recommended):** curl|sh one-liner from the published repo (override via `BLAZAR_REPO`); Windows via winget (rustup provisioning included) or `scripts/install.ps1`; also Homebrew and Scoop packages (`packaging/`).
2. **Service:** `scripts/install.sh` can install a systemd unit; unit sets `RestartPreventExitStatus=3` so bind-conflict exits (code 3) never restart-loop.
3. **Run:** `blazar serve` (alias `start`) — binds `127.0.0.1:11435` by default (blazar's own port, never ollama's 11434; set `port = 11434` for an ex-ollama drop-in); port 0 = ephemeral.
4. **Upgrade:** `blazar upgrade [--version X] [--dry-run]` — sha256-verified self-upgrade.
5. **Rollback engines:** `blazar engine rollback` / `blazar engine use <tag>` — side-by-side tags, never destructive.

### GPU driver preflight (installer)

Before the engine lands, the installer censuses PCI GPU hardware (`lspci`, provisioned when missing) and — when the driver userspace is absent — installs it from **first-party distro repos only** (announce-then-act): NVIDIA via apt's `nvidia-driver` / pacman's `nvidia nvidia-utils`; dnf auto-installs `akmod-nvidia` only when RPMFusion is already enabled; zypper and all third-party-only lanes print the exact command instead of running it. AMD/Intel without a Vulkan ICD get the mesa Vulkan drivers. Every install ends with the loud chain: **REBOOT, then `blazar engine update`** — which auto-picks the newest CUDA build the installed driver supports (runtimes are bundled; no CUDA toolkit is ever installed). Opt out with `BLAZAR_AUTO_DRIVER=0`. Windows is advise-only: the PowerShell installer detects NVIDIA hardware without `nvidia-smi` and links the driver download. `blazar doctor` warns (`nvidia driver` / `vulkan driver` rows) whenever PCI GPU hardware is present but its driver is not.

## Feature flags and configuration switches

All switches live in `~/.config/blazar/config.toml`; manage via `blazar config set/get/unset/list/defaults/edit`. Defaults below are the **live output of `blazar config defaults`** (the binary's own serialization — authoritative). Keys marked *(optional)* exist in `config.rs` but are omitted from the defaults dump while unset.

### Serving & lifecycle

| Key | Default | Purpose |
|---|---|---|
| `host` / `port` | `127.0.0.1` / `11435` | bind address (blazar's own port; `port = 11434` = opt-in ex-ollama drop-in; `0` = ephemeral) |
| `default_ctx` | `16384` | default context size |
| `max_loaded_models` | `0` | concurrent model cap (0 = auto from hardware probe, bytes-based VRAM admission) |
| `idle_sleep_secs` / `idle_timeout_secs` | `300` / `1800` | child-native GPU sleep → SIGTERM eviction ladder |
| `agent` | `false` | agent-oriented defaults |
| `sessions` | `true` | slot KV checkpoints (`--slot-save-path`) |
| `session_bank` | `true` | auto `_auto-<ctx>` bank on evict, restore on respawn (>512 MiB banks dropped) |
| `session_keep_secs` | `900` | session pin lifetime (0 = off) |
| `router` / `router_max_models` | `false` / `0` | one child serves all models (`--models-preset` INI; 0 = upstream default 4) |
| `engine_check_secs` | `86400` | update-currency check cadence (0 = off; check-only, never auto-install) |
| `models_autoload` | *(optional)* | autoload model list at startup |
| `model_load_timeout_secs` | *(optional)* | per-kind defaults (180 s precompiled / 600 s sglang first-JIT); a pin overrides all lanes |

### Memory, KV & context

| Key | Default | Purpose |
|---|---|---|
| `cache_ram_mb` | `8192` | weights-mmap cap (auto-capped at 30% physical RAM) |
| `cache_type` | `""` | KV quant: auto ladder f16→q8_0→q4_0 at 90% VRAM, or explicit f32/f16/bf16/q8_0/q4_0/q4_1/iq4_nl/q5_0/q5_1 |
| `kv_unified` | *(optional)* | unified KV pool (device-backed shared buffer; auto-emits when slots > 1) |
| `kv_unified_per_slot` | `0` | per-slot unified pool sizing |
| `swa_full` | `false` | full-attention SWA override |
| `ctx_checkpoints` | `0` | context checkpoints |
| `ctx_extend` | `0.0` | YaRN context extension factor |
| `no_kv_offload` | `false` | disable KV offload |
| `load_mode` | `""` | mmap / mlock / direct-io |
| `spawn_mem_guard` | `true` | refuse load when MemAvailable < model/2 + 512 MiB |
| `keep_tokens` | `0` | KV keep-token floor |
| `no_host` | `false` | suppress host info in child |
| `context_shift` | `false` | emit `--context-shift` only when true (upstream default is disabled since b10833) |

### Slots, parallelism & determinism

| Key | Default | Purpose |
|---|---|---|
| `slots` | `0` | parallel slots (0 = auto: capacity-aware clamp by KV budget + trained ctx) |
| `deterministic` | `true→forces slots=1` | default `false`; multi-slot perturbs near-tie logits |
| `slot_prompt_similarity` | `0.0` | slot pick by prefix similarity ((0..=1], 0 = engine default 0.1) |
| `adaptive_slots` | `true` | adopt slots+1 after ~60 s sustained concurrency (cap 4) |
| `cache_idle_slots` | `true` | keep idle slots warm |
| `repack` | `true` | weight repack for batch-1 decode |
| `batch_size` / `ubatch_size` | `0` / `0` | physical/max batch (0 = engine default; raising `-ub` above 512 = measured regression — leave alone) |
| `cpu_moe_n` / `cpu_ffn_n` | `0` | MoE experts / FFN layers on CPU |

### Speculative decoding

| Key | Default | Purpose |
|---|---|---|
| `spec` | `auto` | family: off / mtp / eagle3 / dflash / dspark / ngram* (target-side verified-lossless) |
| `spec_cache` | `true` | persistent ngram lookup-cache-dynamic per model |
| `spec_draft_cpu_range` … `spec_draft_*` | see dump | draft-model placement: `cpu_range`, `cpu_strict`, `device`, `ngl`, `threads`, `prio`, `prio_batch`, `cpu_strict_batch`, `threads_batch`, `type_k`/`type_v`, `override_tensor`, `n_cpu_moe`, `cpu_moe` |
| `spec_draft_backend_sampling` | `true` | sampling on the backend |
| `spec_draft_p_min` / `spec_draft_p_split` / `spec_draft_poll` / `spec_draft_poll_batch` | *(optional)* | draft acceptance/split/poll tuning (p_split default 0.10) |
| `ngram_size_m` / `ngram_size_n` / `ngram_min_hits` | `0` | ngram/ngram-map-k/k4v families |
| `ngram_mod_n_match` / `ngram_mod_n_max` / `ngram_mod_n_min` | `0` / `0` / `0` | mod-n window (bounds 1..=1024-class) |
| `adaptive_decay` / `adaptive_target` | `0` / `0.0` | adaptive spec tuning |

### Reasoning & thinking

| Key | Default | Purpose |
|---|---|---|
| `reasoning` | `""` | on / off / auto |
| `reasoning_format` | `""` | none / deepseek / deepseek-legacy |
| `reasoning_effort` | `""` | minimal / low / medium / high / xhigh / max (bridged into chat_template_kwargs, chat lane only) |
| `reasoning_budget` | `-1` | -1 unrestricted / 0 immediate end / N budget |
| `reasoning_budget_message` | `""` | message shown when budget hits |

### Vision & embeddings

| Key | Default | Purpose |
|---|---|---|
| `mmproj_policy` | *(optional)* | lazy (default; respawn w/ projector on first vision request) / attach / skip; bool compat true=attach false=skip; explicit `-mm` in extra_args wins |
| `mmproj_offload` / `mmproj_auto` | `true` / `true` | projector offload / auto-attach |
| `mmproj_device` | `""` | projector device |
| `image_max_tokens` / `image_min_tokens` / `mtmd_batch_max_tokens` | `0` | vision token budgets |
| `embd_normalize` | `0` | embedding normalization |
| `late_chunking_max_tokens` | `8192` | late-chunking embed cap (413 beyond; per-model `late_chunking=true` opt-in) |
| `video_ffmpeg_dir` / `video_fps` / `video_timestamp_interval` | `""` / `0.0` / `0.0` | video input (ffmpeg-capable vision models) |

### YaRN

| Key | Default | Purpose |
|---|---|---|
| `yarn_orig_ctx` / `yarn_ext_factor` / `yarn_attn_factor` / `yarn_beta_fast` / `yarn_beta_slow` | `0` / `-1.0` / `0.0` / `0.0` / `0.0` | YaRN rope-scaling knobs |

### Scheduling & CPU affinity

| Key | Default | Purpose |
|---|---|---|
| `cpu_range` / `cpu_strict` | `""` / `false` | pin child CPU set |
| `prio` / `prio_batch` / `poll` / `poll_batch` | `0` / `0` / `0` / *(optional)* | scheduler priority/poll knobs |
| `threads_http` / `threads_batch` | `0` | HTTP server / batch threads (0 = engine default) |
| `numa` | `""` | distribute / isolate |
| `main_gpu` / `split_mode` / `tensor_split` | `-1` / `""` / `""` | multi-GPU: primary, none/layer/row/tensor, ratios e.g. "3,1" |
| `devices` | `[]` | device pinning (names from `blazar doctor`) |
| `override_tensor` | `[]` | per-tensor backend overrides |
| `tensor_preset` | `""` | "" or "moe-cpu-offload" (experts→CPU) |
| `op_offload` | *(optional)* | op-level offload |
| `check_tensors` | `false` | tensor validation at load |

### Child transport & HTTP

| Key | Default | Purpose |
|---|---|---|
| `child_transport` | `tcp` | child transport: `tcp` (loopback `host:port`) or `unix` (per-child `run/<name>.sock`, Linux/macOS/BSD only) |
| `child_auth` | *(optional)* | unset = auto (TCP children authed, UDS not) / true / false; per-child secret at `run/<model>.apikey` |
| `sse_ping_interval` | *(optional)* | SSE ping (-1 disables; upstream 30) |
| `server_timeout_secs` | *(optional)* | child HTTP timeout (upstream 3600) |
| `chat_template_kwargs` | *(optional)* | JSON object, shape-validated |
| `cont_batching` | *(optional)* | true→`--cont-batching`, false→`--no-cont-batching`, unset = engine default |
| `reuse_port` | `false` | SO_REUSEPORT |
| `lora_init_without_apply` | `false` | LoRA init behavior |
| `warmup` | `true` | post-spawn warmup pass |
| `samplers` | `""` | sampler chain (top_k;top_p;typical); `--sampler-seq` deliberately not a knob |
| `engine_env` | table | extra env for children + probes |

**`child_transport = "unix"`** — spawn children on Unix-domain sockets instead
of loopback TCP: each child listens on `run/<name>.sock` in the daemon's run
directory (next to its pidfile), and the gateway dials it over the same
socket, skipping the network stack and per-request port allocation. Socket
paths are deterministic per model, swept for stale files at daemon boot, and
unlinked when the child is evicted. Path budget is the OS `sun_path` limit
(~104B): a model name deep under a long `data_dir` fails fast with a
teaching error naming the offending component. Not available on Windows
(config validation rejects it there). `tcp` remains the default and is
byte-identical to previous releases.

### Engines, downloads & updates

| Key | Default | Purpose |
|---|---|---|
| `engine_asset` | `auto` | asset flavor selection |
| `update_channel` | `latest` | latest (incl. prereleases) / stable — a pin, not a floor |
| `download_connections` | `8` | parallel byte-range download (≥32 MiB; 1 = single-stream resume; Range-rejecting servers forced single) |
| `rpc_servers` | `""` | llama.cpp RPC backend servers |
| `lazy_mode` | `auto` | lazy spawn mode |
| `fork_retire_days` | `7` | days after full-architecture supersede before a curated fork lane auto-deletes (0 = never; user forks and pinned lanes never auto-delete) |
| `capability_registry_url` | upstream default | curated capability-lane catalog; `""` disables lane discovery entirely |

### Request-path behavior

| Key | Default | Purpose |
|---|---|---|
| `singleflight` | `true` | coalesce identical NON-STREAM chat (5 s bound) |
| `prompt_preflight` | `true` | refuse unfit prompts (exact /tokenize when estimate >90% of ctx) |
| `predictive_preload` | `false` | predictive model load (LC1) |
| `sentinel` / `sentinel_stall_secs` / `sentinel_enforce` | `true` / `30` / `false` | quality observation / stall window / hard 422 mode (per-request `X-Blazar-Enforce` override) |
| `child_header_timeout_secs` | `120` |
| `decode_policy` | `free` | `ollama_compat` lane decode constraint: `free` lets the model choose prose or a tool call; `strict` attaches a GBNF grammar confining tool-bearing completions to whitelisted `<tool_call>` envelopes (names must be request tools, arguments structurally valid JSON) | ceiling for awaiting child response headers; wedged child evicted + one in-band retry; 0 disables (blanket transport ceiling only) |
| `raw_lane_max_tokens` | `2048` | server-side cap for ollama_compat raw-completion generations when the caller omits (or exceeds) a cap; 0 disables (overlay wins over global) |
| `pii_scrub` | `false` | redact emails/bearer secrets/IPv4 in why/watch |
| `reasoning` interplay | — | see Reasoning table |

### Security, TLS & network

| Key | Default | Purpose |
|---|---|---|
| `keys` | `[]` | `[[keys]]`: name/key/models/rpm/tpm/daily_tokens/max_concurrent/weight — non-empty list activates auth |
| `tls_cert` / `tls_key` | `""` | PEM pair (both-or-neither) |
| `cors_origins` | `[]` | browser CORS policy (empty = no ACAO ever; "*" = any) |
| `remotes` | `[]` | `[[remotes]]`: name/url/key; `<remote>:<model>` routing; duplicate names = pool (3 fails → 30 s markdown, least-inflight LB, prefix stickiness) |
| `server_tools` | *(optional)* | EXPERIMENTAL server-side tools (CORS→localhost when set) |
| `server_tools_runtime` | *(optional)* | docker:/podman:/ssh: runtime for tools |
| `mcp_servers_config` / `mcp_servers_json` | *(optional)* | MCP server defs (file vs inline JSON, mutually exclusive) |

### Observability

| Key | Default | Purpose |
|---|---|---|
| `log_level` | *(optional)* | INFO default; `RUST_LOG` wins |
| `audit_log` | `false` | identity+outcome audit jsonl (`<data>/log/audit.jsonl`, 16 MiB rotation) |
| `otlp_endpoint` / `otlp_service` | `""` / `"blazar"` | OTLP traces/metrics export |
| `lookup_cache_static` / `lookup_cache_dynamic` | *(optional)* | read-only / refreshed lookup caches (must exist) |

### Tables

| Table | Purpose |
|---|---|
| `[warm_peg]` | `default = true`; per-engine overrides: `sglang` (blocking peg), `llamacpp` (detached peg), mistral.rs and sdcpp never peg |
| `[sdcpp]` | *(no section — by design)* diffusion tuning rides request parameters (steps/cfg/seed/size on `POST /v1/images/*`), not config knobs; spawn flags (`--vae`/`--llm`/`--t5xxl`...) come from the model's component set; `extra_args` passes manifest-probed flags (`--backend clip=cpu,vae=cuda0,...`, `--lora-model-dir`, `--vae-tiling`...) |
| `[engine_routing]` | `mode = "auto"\|"manual"` (auto = per-model routing; manual = byte-identical single active engine), `policy = "quality"\|"latency"\|"throughput"` |
| `[semantic_cache]` | `enabled = false`, `ttl_secs = 600`, `threshold = 0.9`, `max_entries = 256` (opt-in; per-request `X-Blazar-Cache` overrides) |
| `[mistralrs]` | `mistralrs_pa_memory_fraction` (0.05..=0.95), `mistralrs_paged_attn` (false = classic KV sized from ctx — fits big vision models on 8 GB) + MistralrsTuning knobs |
| `[sglang]` | SglangTuning knobs (`cache_report` defaults ON for blazar-managed children — it powers `ps` HIT and per-model metrics; set `false` to opt out; `radix_eviction_policy` lru/lfu/slru/priority, `session_radix_cache`, `mixed_chunk`, …); VRAM ladder flags (`--mem-fraction-static`, `--cpu-offload-gb`, `--kv-cache-dtype`, `--context-length`) owned by the profile compiler; explicit `mem_fraction_static`/`kv_cache_dtype` pins win |
| `[model_overrides.<name>]` | per-model REPLACEMENTS: ctx, slots, deterministic, spec, lazy_mode, loras, extra_args (manifest-validated), cache_type, kv_unified, ctx_extend, cpu_moe_n, cpu_ffn_n, override_tensor, devices, warmup, reasoning_budget, reasoning_effort, reasoning, replicas (>1 = prefix-affinity routing), pin (skip in capacity-eviction victim filter), chat_template (XOR chat_template_file), sampler_defaults, spm_infill, late_chunking, rpc_servers, mmproj, sglang, mistralrs, engine (tag or kind; wins over both routing modes) — `deny_unknown_fields`: typos fail loudly |

Full key grammar and examples: `blazar config --help` (`CONFIG_EXAMPLES`, `CONFIG_SET_EXAMPLES`), and `blazar config defaults` prints the live default file.

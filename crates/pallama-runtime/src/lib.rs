//! Pallama runtime: async engine management, HF client, process
//! supervision, event bus, bench runner.

pub mod placeholder {
    // Modules land in steps B-E: hf, engine, supervisor, bench, events.
}
